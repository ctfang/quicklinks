services:
  navihub:
    image: debian:bookworm-slim
    working_dir: /app
    volumes:
      - ./app:/app/app
    environment:
      - PORT=3000
    ports:
      - "3000:3000"
    command: ["/app/app"]
